[global]
    markup = yes
    font = "Inconsolata Bold 16"
    format = "<span font="Inconsolata Bold 16">%s</span>\n<span font="Inconsolata Bold 12">%b</span>"
    alignment = center

    bounce_freq = 0
    follow = mouse
    offset = 10x30
    width = 400
    height = 300
    horizontal_padding = 8
    idle_threshold = 120
    ignore_newline = false
    indicate_hidden = false
    line_height = 0
    monitor = 0
    padding = 8
    show_age_threshold = 60
    show_indicators = true
    shrink = no
    sticky_history = true
    sort = yes
    transparency = 5
    word_wrap = yes

    icon_position = left
    icon_path = /home/srsantos/.config/dunst/icons
    max_icon_size = 96

    frame_width = 1
    frame_color = "#323232"
    separator_color = "#1A1A1A"
    separator_height = 4
    ellipsize = middle

### Progress bar ###

    # Turn on the progess bar. It appears when a progress hint is passed with
    # for example dunstify -h int:value:12
    progress_bar = true

    # Set the progress bar height. This includes the frame, so make sure
    # it's at least twice as big as the frame width.
    progress_bar_height = 6

    # Set the frame width of the progress bar
    progress_bar_frame_width = 1

    # Set the minimum width for the progress bar
    progress_bar_min_width = 150

    # Set the maximum width for the progress bar
    progress_bar_max_width = 400

    # Corner radius for the progress bar. 0 disables rounded corners.
    progress_bar_corner_radius = 0

    browser = /usr/bin/luakit
    dmenu = SSH_AUTH_SOCK=/run/user/1000/gnupg/S.gpg-agent.ssh /usr/bin/rofi -dmenu -p 

    startup_notification = false

    stack_duplicates = true
    hide_duplicate_count = false
    corner_radius = 8

[shortcuts]
    close = ctrl+space
    close_all = ctrl+shift+space
    history = ctrl+shift+grave
    context = ctrl+shift+period

[urgency_low]
    background = "#222222"
    foreground = "#BBBBBB"
    timeout = 8

[urgency_normal]
    background = "#222222"
    foreground = "#BBBBBB"
    timeout = 8

[urgency_critical]
    background = "#770000"
    foreground = "#EEEEEE"
    frame_color = "#1B1B1B"
    timeout = 0

[Xfce volume daemon]
    appname = Xfce volume daemon
    format = "<span font="Inconsolata Bold 16">%s</span><span font="Inconsolata Bold 12">%b</span>"

[pasystray]
    appname = pasystray
    format = "<span font="Inconsolata Bold 16">%s</span><span font="Inconsolata Bold 12">%b</span>"

[update_arch_notification.sh]
    summary = "No updates for Archlinux"
    format = "<span font="Inconsolata Bold 16">%s</span><span font="Inconsolata Bold 12">%b</span>"

[update_aur_notification.sh]
    summary = "No updates from AUR"
    format = "<span font="Inconsolata Bold 16">%s</span><span font="Inconsolata Bold 12">%b</span>"

[ChangeBrightness]
    appname = notify-send.sh
    format = "<span font="Inconsolata Bold 16">%s</span><span font="Inconsolata Bold 12">%b</span>"



